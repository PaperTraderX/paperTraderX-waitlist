/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

// This file lets you customize Tailwind (optional). You only need to touch it if you want to:
// ✅ Add custom colors, fonts, or breakpoints
// ✅ Extend Tailwind’s default theme
